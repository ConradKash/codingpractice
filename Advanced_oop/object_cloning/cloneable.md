For a class to be clonable it must implement the cloneable interface.

- `cloning` vs `copying`
  For copying it means that you just copy the reference of the object to the new obj. That means if you change the value of the second object you are changing the value of that was referencing.
